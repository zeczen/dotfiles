set relativenumber

" Use 4 spaces for tabs
set tabstop=4
set shiftwidth=4
set expandtab

" Enable auto-indentation and smart indenting
set autoindent
set smartindent

" Highlight search results as you type
set incsearch

" Ignore case when searching
set ignorecase

" Make search case-sensitive if uppercase characters are used
set smartcase
